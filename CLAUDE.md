Global working rules live in `~/.claude/CLAUDE.md` and load automatically in every session:

- Mandatory plan-first workflow (plan → Codex review → implement via Codex)
- Codex `exec` CLI dispatch conventions
- Don't defer to dodge friction
- Finish and Verify
- NEVER create or switch branches
- NEVER edit files in any `-dist` package directory

This file is intentionally minimal — add only sheets-finance-mcp-specific guidance here.
